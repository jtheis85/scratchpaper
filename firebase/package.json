{
  "name": "application-name",
  "version": "0.0.1",
  "dependencies": {
    "firebase": "^2.4.2"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "watchify": "^3.7.0"
  },
  "scripts": {
    "build": "browserify main.js -o bundle.js"
  }
}
